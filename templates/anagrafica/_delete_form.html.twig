<form method="post" action="{{ path('app_anagrafica_delete', {'id': anagrafica.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ anagrafica.id) }}">
    <button class="btn">Delete</button>
</form>
