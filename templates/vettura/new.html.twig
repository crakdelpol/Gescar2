{% extends 'base.html.twig' %}

{% block title %}Nuovo Veicolo – Gescar{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2"><i class="bi bi-plus-circle me-2"></i>Nuovo veicolo</h1>
    <a href="{{ path('app_vettura_index') }}" class="btn btn-sm btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i>Lista veicoli
    </a>
</div>

<div class="card">
    <div class="card-body">
        {{ include('vettura/_form.html.twig') }}
    </div>
</div>
{% endblock %}
