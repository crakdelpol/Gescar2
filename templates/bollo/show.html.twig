{% extends 'base.html.twig' %}

{% block title %}Bollo{% endblock %}

{% block body %}
    <h1>Bollo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bollo.id }}</td>
            </tr>
            <tr>
                <th>DataScadenzaBollo</th>
                <td>{{ bollo.dataScadenzaBollo ? bollo.dataScadenzaBollo|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ bollo.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bollo_index') }}">back to list</a>

    <a href="{{ path('app_bollo_edit', {'id': bollo.id}) }}">edit</a>

    {{ include('bollo/_delete_form.html.twig') }}
{% endblock %}
