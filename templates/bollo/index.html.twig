{% extends 'base.html.twig' %}

{% block title %}Bolli – Gescar{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2"><i class="bi bi-receipt me-2"></i>Bolli</h1>
    <a href="{{ path('app_bollo_new') }}" class="btn btn-primary btn-sm">
        <i class="bi bi-plus-lg me-1"></i>Nuovo bollo
    </a>
</div>

<div class="table-responsive small">
    <table class="table table-striped table-hover table-sm align-middle">
        <thead class="table-dark">
            <tr>
                <th>Veicolo</th>
                <th>Intestatario</th>
                <th>Scad. Bollo</th>
                <th class="text-end">Azioni</th>
            </tr>
        </thead>
        <tbody>
        {% set oggi = "now"|date("Y-m-d") %}
        {% for bollo in bollos %}
            <tr>
                <td>
                    {% if bollo.vettura %}
                        <a href="{{ path('app_vettura_show', {'id': bollo.vettura.id}) }}" class="text-decoration-none fw-semibold">
                            {{ bollo.vettura.targa ?: '–' }}
                        </a>
                        <span class="text-muted ms-1">{{ bollo.vettura.marca }} {{ bollo.vettura.modello }}</span>
                    {% else %}–{% endif %}
                </td>
                <td>
                    {% if bollo.vettura and bollo.vettura.intestatario %}
                        <a href="{{ path('app_anagrafica_show', {'id': bollo.vettura.intestatario.id}) }}" class="text-decoration-none">
                            {{ bollo.vettura.intestatario.cognome | capitalize }} {{ bollo.vettura.intestatario.nome | capitalize }}
                        </a>
                    {% else %}–{% endif %}
                </td>
                <td>
                    {% if bollo.dataScadenzaBollo %}
                        {% set scad = bollo.dataScadenzaBollo|date("Y-m-d") %}
                        {% if scad < oggi %}
                            <span class="badge bg-danger">{{ bollo.dataScadenzaBollo|date('d/m/Y') }}</span>
                        {% elseif scad < ("now +30 days")|date("Y-m-d") %}
                            <span class="badge bg-warning text-dark">{{ bollo.dataScadenzaBollo|date('d/m/Y') }}</span>
                        {% else %}
                            <span class="text-muted">{{ bollo.dataScadenzaBollo|date('d/m/Y') }}</span>
                        {% endif %}
                    {% else %}<span class="text-muted">–</span>{% endif %}
                </td>
                <td class="text-end text-nowrap">
                    <a href="{{ path('app_bollo_show', {'id': bollo.id}) }}" class="btn btn-sm btn-outline-secondary" title="Visualizza">
                        <i class="bi bi-eye"></i>
                    </a>
                    <a href="{{ path('app_bollo_edit', {'id': bollo.id}) }}" class="btn btn-sm btn-outline-primary" title="Modifica">
                        <i class="bi bi-pencil"></i>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4" class="text-center text-muted py-4">
                    <i class="bi bi-receipt me-2"></i>Nessun bollo trovato.
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
